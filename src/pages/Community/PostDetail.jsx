const Wrapper = styled.div`
  min-height: 100vh;
  background-color: #b4d0e9;
`

const PostDetail = () => {
  // 라우터로 부터 게시글 번호를 받아온다
  // const { postId } = useParams()

  return <Wrapper>sdfsdf</Wrapper>
}
export default PostDetail
